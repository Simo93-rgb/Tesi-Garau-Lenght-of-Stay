% capitoli/04_risultati.tex - Capitolo 4: Risultati

\chapter{Risultati}
\label{ch:risultati}

\section{Metriche di Classificazione}
La validazione del modello predittivo \texttt{bert-base-uncased}, architettato sull'embedding a \textit{Storytelling} per la classificazione dicotomica sul Length of Stay lungo, ha prodotto le seguenti metriche di classificazione.

Al termine del processo di hyperparameter tuning e addestramento supervisionato, la metrica chiave di valutazione, la \textbf{Balanced Accuracy}, si è assestata su valori di circa l'\textbf{88\%}. Questo risultato assume particolare rilievo considerando il gravoso sbilanciamento delle classi in input: la Balanced Accuracy, di fatto media aritmetica delle accuratezze (Sensibilità e Specificità) sulle due classi in esame (Casi normali vs Casi patologici), testimonia che il classificatore ha evitato di \textbf{collassare sulla classe maggioritaria} (la degenza breve). Un ruolo critico nel conseguimento di quest'alta soglia sensibile è ascrivibile all'adozione differenziale e decisa della Focal Loss, che dinamicamente forzava il focus iterativo di addestramento primariamente sulle tracce rare a LOS anomalo, contribuendo a limitare i falsi negativi e mitigando il bias di predizione introdotto dagli esemplari maggioritari.

\section{Confronto Prestazionale degli Embedding}
Prima di eleggere lo \textit{Storytelling} come soluzione definitiva, il modello è stato regolarmente addestrato e testato sulle altre due formulazioni di embedding discusse (Coppie Azione-Token Temporale e Array Concatenati). La Tabella \ref{tab:metrics_comparison} riassume il confronto sulle principali metriche di classificazione binaria. Lo Storytelling ha garantito un balzo in avanti, specialmente nella metrica chiave di \textit{Balanced Accuracy}.

% PLACEHOLDER CAPITOLO 04 - PARAGRAFO 4.2 - TABELLA METRICHE EMBEDDING
\begin{table}[htbp]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Embedding} & \textbf{Accuracy} & \textbf{Balanced Accuracy} & \textbf{F1-Score} & \textbf{Precision} & \textbf{Recall} & \textbf{Specificity} \\
        \hline
        Azioni + Binning   & 91.34\%           & 87.16\%                    & 67.35\%           & 57.23\%            & 81.82\%         & 92.51\% \\
        Array Concatenati  & 87.29\%           & 83.44\%                    & 57.40\%           & 45.24\%            & 78.51\%         & 88.36\% \\
        Storytelling       & 92.61\%           & 89.69\%                    & 71.72\%           & 61.54\%            & 85.95\%         & 93.42\% \\
        \hline
    \end{tabular}%
    }
    \caption{Confronto delle prestazioni di classificazione per le tre tecniche di embedding testate.}
    \label{tab:metrics_comparison}
\end{table}

Per facilitare l'analisi visiva delle performance, la Figura \ref{fig:confronto_metriche_punti} espone graficamente i differenziali riportati in tabella.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{"img/confronto_metriche_punti.png"}
    \caption{Rappresentazione grafica a dispersione delle metriche di classificazione a confronto per i tre metodi di embedding testati: \textit{Storytelling}, Azioni + Binning e Array Concatenati. Il grafico illustra visivamente i risultati tabellari, evidenziando il vantaggio prestazionale garantito dall'approccio narrativo su tutte le metriche chiave, in particolar modo sulla \textit{Balanced Accuracy} e sull'\textit{F1-Score}.}
    \label{fig:confronto_metriche_punti}
\end{figure}
\FloatBarrier

\subsection{Analisi Critica: Il Trade-Off tra Precision e Recall}
Nell'ottica clinica applicata, le evidenze restituite dalla tabella non devono essere valutate unicamente sotto la lente della \textit{Balanced Accuracy}, seppur eccellente. Il comportamento profondo del modello è nitidamente leggibile scomponendo il risultato nelle metriche di \textbf{Precision (61.54\%)} e \textbf{Recall (85.95\%)} ottenute con la variante \textit{Storytelling}.

Il parametro Recall (Sensibilità) si dimostra volutamente prominente: la rete è stata addestrata e calibrata -- anche mediante l'impiego della Focal Loss -- per captare con elevata affidabilità i casi positivi, ovvero l'insorgenza di anomalie logistiche (pazienti con lunghissimo Length of Stay). Questo si traduce in una ridottissima generazione di \textit{falsi negativi}. Nel contesto direzionale di una struttura nosocomiale, un falso negativo costituisce un danno economico e gestionale severo, in quanto concorre ad una categorizzazione ottimistica di un iter che sfocerà invece in percorsi laboriosi, saturando letti d'appoggio imprevisti, complicazioni inaspettate o dimissioni abortite.

Di contro, la \textit{Precision} al 61.54\% implica un margine intrinseco di \textbf{falsi positivi}. Dal punto di vista pratico, ciò comporta che una porzione decrescente di degenti classificati come "ad alto rischio di LOS prolungato" dall'intelligenza artificiale concluderà in realtà il proprio percorso nei tempi previsti. In questo specifico dominio di indagine, il \textit{trade-off} è non solo ampiamente giustificato, ma ricercato proattivamente: il costo etico ed amministrativo legato all'attivazione di monitoraggi supplementari e allocation preventiva su un "falso allarme" è ordini di grandezza inferiore rispetto al dramma di trascurare e smarrire analiticamente un paziente esposto ad altissime complessità di cura. Questo bilanciamento matematico certifica quindi non solo la robustezza formale di BERT, ma la reale fattibilità della sua adozione quale \textit{Decision Support System} proattivo per le direzioni sanitarie, massimizzando strutturalmente la tutela clinica al prezzo di parziali ma tollerabili sovrastime logistiche d'allerta.

A ulteriore riprova delle differenze di convergenza, si riportano le \textbf{Matrici di Confusione} relative ai test set di ciascun approccio. La capacità dello Storytelling di limitare i falsi negativi patologici giustifica l'investimento computazionale del fine-tuning linguistico.

% MATRICI DI CONFUSIONE: CONFRONTO
\begin{table}[htbp]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \textbf{Array Concatenati}\par\vspace{0.2cm}
        \resizebox{\textwidth}{!}{%
        \begin{tabular}{cc|c|c|}
          & \multicolumn{1}{c}{} & \multicolumn{2}{c}{\textbf{Predetto}} \\
          & \multicolumn{1}{c}{} & \multicolumn{1}{c}{Normale} & \multicolumn{1}{c}{Critico} \\
          \cline{3-4}
          \multirow{2}*{\textbf{Reale}} & Normale & 873 & 115 \\
          \cline{3-4}
          & Critico & 26 & 95 \\
          \cline{3-4}
        \end{tabular}%
        }
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \textbf{Binning}\par\vspace{0.2cm}
        \resizebox{\textwidth}{!}{%
        \begin{tabular}{cc|c|c|}
          & \multicolumn{1}{c}{} & \multicolumn{2}{c}{\textbf{Predetto}} \\
          & \multicolumn{1}{c}{} & \multicolumn{1}{c}{Normale} & \multicolumn{1}{c}{Critico} \\
          \cline{3-4}
          \multirow{2}*{\textbf{Reale}} & Normale & 914 & 74 \\
          \cline{3-4}
          & Critico & 22 & 99 \\
          \cline{3-4}
        \end{tabular}%
        }
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \textbf{Storytelling}\par\vspace{0.2cm}
        \resizebox{\textwidth}{!}{%
        \begin{tabular}{cc|c|c|}
          & \multicolumn{1}{c}{} & \multicolumn{2}{c}{\textbf{Predetto}} \\
          & \multicolumn{1}{c}{} & \multicolumn{1}{c}{Normale} & \multicolumn{1}{c}{Critico} \\
          \cline{3-4}
          \multirow{2}*{\textbf{Reale}} & Normale & 923 & 65 \\
          \cline{3-4}
          & Critico & 17 & 104 \\
          \cline{3-4}
        \end{tabular}%
        }
    \end{minipage}
    \vspace{0.4cm}
    \caption{Matrici di confusione confrontate sui tre approcci di embedding.}
    \label{tab:confusion_matrices}
\end{table}


\section{Explainable AI: L'Adattamento degli Integrated Gradients}
Mentre le metriche computazionali provano la \textbf{capacità predittiva} del modello generico per la classificazione dei log ospedalieri, l'aspetto metodologicamente distintivo del progetto risiede nella trasparenza offerta dalle interpretazioni di classe post-hoc. 

\subsection{Giustificazione del Metodo e Gradient Shattering}
Per interpretare le logiche decisionali di BERT, lo studio ha adottato il rigore degli \textbf{Integrated Gradients (IG)}. La scelta di questo algoritmo interpretativo primario, scartando alternative euristiche storiche diffuse in letteratura, è dettata da stringenti limiti matematici e di dominio. Tecniche come Grad-CAM risultano intrinsecamente legate alle features map spaziali delle Reti Convoluzionali (CNN) e si adattano con grandi limiti deduttivi alla rigidità vettoriale dei meccanismi asincroni di \textit{Self-Attention} alla base del transformer testuale. Analogamente, approcci appoggiati all'estrazione di gradienti standard (Vanilla Gradient Descent) o fondati su perturbazioni con vettori di iniezione a rumore continuo (SmoothGrad, Blur IG), pur confermandosi campioni d'analisi su \textit{Computer Vision}, non si interfacciano coerentemente alla natura discreta imposta dal \textit{Word Embedding} di un modello linguistico. A riguardo, un confronto visuale astratto sulle metriche (Figura \ref{fig:ig_comparison}) rivela plasticamente il disturbo di calcolo associato a pattern basici che verrebbe inevitabilmente trasferito sulla parola di testo clinica.

Un ulteriore limite del calcolo iterativo classico della retropropagazione standard è il \textbf{Gradient Shattering} (frantumazione del gradiente). Nelle reti neurali profonde, i gradienti calcolati localmente attraverso funzioni di attivazione non lineari (come la \textit{GELU}) possono annullarsi o divergere in punti specifici della rete, producendo attribuzioni localmente incoerenti e prive di significato globale \cite{sundararajan2017axiomatic}.
Valutando invece l'integrale definito lungo l'intero percorso dalla baseline all'input reale, gli Integrated Gradients mediano queste oscillazioni, restituendo per ogni sub-token un'attribuzione netta che riflette il contributo effettivo all'output di classificazione finale.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{img/comparison_with_a_dog.png}
    \caption{Confronto tra metodi di Feature Attribution XAI applicati all'individuazione di un input ``cane''. I metodi \textit{Vanilla Gradient} e \textit{Guided BackProp} producono maschere ad alto rumore geometrico, prive di valore interpretativo. Per evitare lo stesso problema sul corpus clinico testuale, si è adottato il calcolo integrale degli \textit{Integrated Gradients}.}
    \label{fig:ig_comparison}
\end{figure}
\FloatBarrier

\subsection{Approssimazione Discreta e Completeness}
Dal punto di vista implementativo, il calcolo continuo dell'integrale degli IG non può essere risolto in forma chiusa per reti neurali profonde. L'integrale viene valutato in modo discreto tramite una somma di Riemann con $m$ step di interpolazione. La formula approssimata, adottata come standard nelle librerie come \textit{Captum}, è la seguente:

\begin{equation}
IntegratedGrads_i^{approx}(x) \Coloneqq (x_i - x'_i) \times \frac{1}{m} \sum_{k=1}^{m} \frac{\partial F(x' + \frac{k}{m} \times (x - x'))}{\partial x_i}
\end{equation}

Nell'adattamento matematico di tale equazione per sequenze linguistiche discrete, il termine $x'$ detiene il ruolo nevralgico della cosiddetta \textbf{Baseline}. Tale parametro funge da punto d'origine astratto, ideato per simulare al classificatore \textit{BERT} una condizione di totale assenza di informazione testuale. Poiché i token di testo non possono essere azzerati matematicamente con un semplice scalare nullo, si è resa necessaria una preparazione tensoriale algoritmica. La baseline è stata creata replicando il \textit{Padding Token} (\texttt{[PAD]}, identificativo $ID = 0$) fino a eguagliare la lunghezza della traccia elaborata (l'input reale $x$). Questo vettore viene propagato in \textit{feedforward} lungo il layer di \texttt{word\_embeddings} del modello, ottenendo la sua rappresentazione nello spazio latente numerico. Fissare la baseline in questo spazio continuo garantisce dunque all'algoritmo di interpolazione uno "zero semantico" affidabile da cui partire per approssimare l'ingresso testuale.

Per valutare matematicamente l'affidabilità delle importanze estratte e assicurarsi che l'approssimazione discreta dell'integrale sia adeguata, la spiegazione sfrutta l'assioma essenziale della \textbf{Completeness} (Completezza). Esso stabilisce che la somma algebrica dei gradienti attribuiti a tutti i singoli token debba eguagliare fedelmente la differenza tra l'output predetto dal modello per l'input in esame $f(x)$ e l'output per la \textit{baseline} isolata $f(x')$. Per svincolare la discrepanza dalla magnitudo della predizione originaria del neurone finale, tale accuratezza calcola un vincolo dimensionale quantificato dall'\textit{Errore Relativo}:
\begin{equation}
\text{Rel\_Error} = \frac{| f(x) - f(x') - \sum \text{Attributions} |}{| f(x) - f(x') |}
\end{equation}
Quando questo errore cala sotto una rigidissima tolleranza del $5\%$ ($\text{Rel\_Error} \le 0.05$), si ritiene di aver raggiunto la chiusura integrativa e pertanto definita la formale \textbf{convergenza} dell'algoritmo.

Per forzare la convergenza, la programmazione ha previsto uno script Python contente una subroutine (\textit{algoritmo adattivo}) che raddoppia l'indice incrementale della base approssimatrice degli iper-parametri di calcolo. Partendo di prassi con una configurazione base di test ($m=50$), il modulo controlla che lo scarto esibito rientri nelle quote minime. Se l'errore travalica il limite, la diagnostica ripiega sul raddoppio dei passaggi (\textit{steps doubling}) ripetendo totalmente la funzione.
Su queste dinamiche protocollari la maggioranza fisiologica ($90\%$) delle derivate giunge a convergenza definitiva a un passo iterativo di precisione quantificati statisticamente a \textbf{1500 steps}. Di contro, tracce anamnestiche insolitamente vaste spingono questo automatismo in escalation arrivando a esigere persino frazioni minime per bloccare l'errore raggiungendo perciò massimali esecutivi di \textbf{5500 steps}.

Questo elevato valore iterativo richiesto per convergere palesa la grande differenza computazionale tra NLP e gli algoritmi di Explainability in Computer Vision, i quali si risolvono spesso con appena 50 o 100 \textit{steps}. In campo visivo l'interpolazione transita lungo lo spettro dei pixel da aree vuote per sfumature matematiche coese; al contrario, il \textit{Word Embedding} archiviato nel \textit{transformer} è una griglia matematica astratta a 768 dimensioni molto ripida e disgiunta tra occorrenze semantiche ben polarizzate limitando severamente il passo di scarto ammissibile senza causare deformità volumetriche all'integrale effettivo. L'alta frizione derivativa esige pertanto l'enorme fittezza numerica del campionamento ($m>1500$) per superare l'assioma.

\subsection{Bottleneck Computazionale}
L'alto numero di \textit{step} introduce un severo sovraccarico prestazionale legato al disallineamento operativo di memoria tra CPU e GPU. La classe \texttt{IntegratedGradients} della libreria Captum processa il campionamento adottando per default la quadratura standard di \textbf{Gauss-Legendre}; una soluzione puramente algebrica per definire i vertici spaziali non equidistanziati che vincola però `numpy` a generare sulla CPU matrici dense di ampiezza $m \times m$ valutandone gli autovalori iterativamente. Allo standard clinico di \textbf{1500 steps}, questa decomposizione frenava l'infrastruttura sprecando fino a 15 secondi per passaggio disattivando contestualmente l'offload grafico della GPU RTX 4090 durante lo svuotamento. A soglie critiche da \textbf{5500 steps} provocava puntualmente il crash strutturale matematico infrangendo il \textit{timeout}.

A livello codice si è pertanto ovviato imponendo forzatamente il calcolo \textit{standard} d'ispezione alla formula di **Riemann Trapezoid** (`method='riemann\_trapezoid'`). Tale implementazione numerica modella i segmenti con equispazialità costante: evitando del tutto lo studio logico matriciale e riversando costanti lineari vettorializzando al cento per cento il carico sulla GPU (\textit{pure-GPU arithmetic}). Ai regimi iper-densi testati l'approssimazione differenziale di quest'ottimizzazione rispetto al gaussiano crolla sotto una tolleranza irrisoria dello $0.1\%$; fornendo insomma garanzie formali intatte risolvendo però criticamente i limiti hardware \textbf{riportando i tempi d'estrazione clinica a circa 2.5 secondi a singolo sample}.

\section{Ricomposizione dei Token e Analisi dell'Impatto Clinico}

\subsection{Il Limite Euristico della Tokenizzazione Sub-Word}
L'applicazione diretta degli Integrated Gradients su uno \textit{Storytelling} elaborato tramite BERT manifesta tuttavia un profondo limite euristico per un decisore medico umano. BERT sfrutta un tokenizer sub-word (tipicamente il \textit{WordPiece}), spezzando semanticamente le parole rare, gli acronimi nosocomiali ed i tag composti generati in fase di data embedding in atomi non autosufficienti concettualmente (sub-token). L'algoritmo IG standard attribuirebbe un punteggio di importanza a singoli sub-token privi di significato semantico autonomo (es. frammenti di acronimi o codici di procedura), rendendo complessa l'interpretazione clinica e vanificando per il management il pregio diagnostico ed ottimizzativo del framework.

\subsection{Strategia di Aggregazione (Aggregation Strategy)}
La presente proposta risolve radicalmente l'impasse introducendo il meccanismo cruciale della \textbf{Ricomposizione Semantica Post-Calcolo} dei token frammentati, che governa la conversione dello score grezzo dell'attribuzione matematica in metrica clinica operativa.

Al fine di intercettare analiticamente gli atti medici da ricostruire, il modulo (\textit{Clinical Action Aggregator}) importa il dizionario in uso (\texttt{translation\_cache.json}). Cosciente che i record clinici affondano radici nella stesura infermieristica italiana ma giungono formattati per la somministrazione al modello in lingua inglese, la procedura Python usa il dizionario incrociandone i valori: la stringa in uscita anglofona è individuata e aggregata alla sua naturale controparte italiana per l'allocazione al clinico del valore esatto dell'importanza vettoriale isolata. Tale macro-fase è pilotata in RAM seguendo l'accorpamento additivo diviso in due logiche consequenziali:
\begin{enumerate}
    \item \textbf{Step A: Da Embedding Dimension a Token Score.} Allo stato primitivo di estrazione back-propagativa post-tuning, l'algoritmo IG classico invocato da Captum non restituisce un singolo valore numerico per ciascun tag alfabetico decodificato, ma diffonde ed espone un valore di calcolo derivato per ogni singola coordinata del vettore denso di codifica. Nell'architettura specifica da noi in analisi (\texttt{bert-base-uncased}), questa scomposizione si declina in ben 768 dimensioni continue distinte per ogni frammento di sillaba. Poiché l'intento applicativo è valutarne l'ingombro processuale finale (la predizione del Length of Stay), le 768 \textit{features score} occulte devono imperativamente subire una compressione quantitativa: il processo effettua vettorialmente una macro-sommatoria algoritmica che attraversa e somma orizzontalmente i valori distribuiti nell'asse tensoriale di profondità (matematicamente, il sistema effettua un'operazione deduttiva di \textit{reduction} imponendo \texttt{sum(dim=-1)} sulla libreria estesa di estrazione). Tramite lo Step A, questa astrazione collassa restituendo un unico scalare oggettivo riflettente l'impatto integrale olistico adoperato dal modello attenzionale per ogni unità.
    \item \textbf{Step B: Da Sub-word frammentato a Parola Clinica Intera.} Una volta isolati gli score scalari per ciascun frammento, è necessario ricomporre la terminologia clinica originale sommando i contributi dei sub-token correlati. Il tokenizer \textit{WordPiece} di BERT segnala i frammenti secondari con il prefisso \texttt{\#\#}: lo script individua queste sequenze e somma algebricamente i loro punteggi al token radice precedente (\textit{root sum}), ricostruendo così la parola clinica intera con un unico score aggregato. Come passo finale, i token artificiali \texttt{[CLS]} e \texttt{[SEP]} vengono esclusi dalla raccolta degli score: pur essendo funzionali al fine-tuning della rete, non corrispondono ad alcuna azione clinica reale e la loro inclusione nel report diagnostico introdurrebbe rumore privo di valore interpretativo.
\end{enumerate}

Conducendo quest'azione meticolosa su un vocabolario testuale raggruppabile attorno ai parametri cardine documentabili dell'ospedale (esami, visite, trattamenti, ecc.), si assegna rigorosamente un punteggio univoco ed aggregato dinamicamente allo specifico atto medico effettivo intra-processuale reale in maniera coerente.

\subsection{Risoluzione End-to-End: Il caso "Cardiology visit"}
A titolo dimostrativo e per cementificare \textit{end-to-end} i costrutti discussi, si consideri la mappatura dell'evento di log processato e testato: \textbf{"Cardiology visit"}. Essendo la traduzione semantica delle degenze effettuata in lingua inglese per assecondare la sintassi per cui è stato istruito \texttt{bert-base-uncased}, la ricostruzione \textit{post-hoc} risulta fondamentale e modella testualmente le 6 sotto-fasi matematiche IG presentate poc'anzi:

\begin{enumerate}
    \item \textbf{Tokenizzazione e Baseline:} Il testo d'ingresso viene prelazionato dal Layer e sfaldato dal \textit{WordPiece} tokenizer in \texttt{[CLS, card, \#\#iology, visit, SEP]}. Per generare l'esca matematica su cui far orbitare la derivata parziale rispetto agli input formattati di riga, viene allocato il \textit{baseline tensor} costituito da un vettore di pari dimensionalità ma saturato esclusivamente dal fattore nullo Padding (ID 0): \texttt{[PAD, PAD, PAD, PAD, PAD]}.
    \item \textbf{Interpolazione a Step Finita:} Per congiungere la baseline fittizia all'input paziente reale in esame, l'integrale approssimato crea $m=50$ frazioni spaziali equidistanti. In ciascuno step discreto $\alpha_i$ (che corre tra $0.00$ e la prossimità asintotica $1.00$) avviene la fusione interpolata e pesata nel continuo per l'indagine algebrica: $\text{Baseline}_{emb} + \alpha_i \times (\text{Input}_{emb} - \text{Baseline}_{emb})$.
    \item \textbf{Attribuzione Sparsa Ponderata:} Su ciascuno dei 50 steps spaziotemporali è valutato iterativamente il \textit{gradiente} attenzionale di logica contro il nodo predittivo finale del modello. Terminate le fasi di inferenza passiva e raccolta retroattiva, l'integrazione numerica distribuisce come ritorno un tensore matematicamente irragionevole per il singolo uomo: ogni sub-token testuale processato (tra cui \texttt{card} e \texttt{\#\#iology}) espone difatti sulla griglia ben $768$ valori \textit{float} nativi.
    \item \textbf{Aggregazione Step A (Collasso Logit):} L'azione coesiva interviene tramite la macro-riduzione $\Sigma_{dim=-1}$, annullando algebricamente l'orizzonte tensoriale isolando l'intensità direzionale netta. Supponendo, a titolo d'esempio esplicativo, di quantificare un attrito generato di $+12.45$ in capo a \texttt{card}, una sfumatura concettuale d'apporto di $+7.83$ isolata sul suffisso \texttt{\#\#iology} e uno spiccato interesse computazionale quantificabile a $+18.92$ ad uso stringa \texttt{visit}. Viene qui forzata anche l'identificazione selettiva in esubero: i sub-token \texttt{CLS} e \texttt{SEP} vengono destituiti dalla pipeline a questo esatto traguardo.
    \item \textbf{Aggregazione Step B (Cucitura Lessicale):} Lo script scansiona la sequenza e individua i sub-token marcati con \texttt{\#\#}: il punteggio di \texttt{\#\#iology} ($+7.83$) viene sommato a quello del token radice \texttt{card} ($+12.45$), producendo il termine clinico ricomposto \textbf{``Cardiology''} con score aggregato $12.45 + 7.83 = 20.28$. Il token \texttt{visit}, non essendo un frammento secondario, mantiene il proprio punteggio invariato ($+18.92$).
\end{enumerate}

Quest'azione algebrica mirata restituisce un set di \textbf{macro-scores unici per atto medico coerente} (Figura \ref{fig:token_recomposition}), che l'architettura espone interamente decodificato e quantificato, superando le mutilazioni linguistiche del Transformer e favorendone lo sviluppo ed ispezione manageriale per le direzioni sanitarie attive.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        node distance=2.5cm,
        box/.style={rectangle, draw=blue!80!black, fill=blue!5, thick, minimum height=1cm, minimum width=2.2cm, rounded corners=2mm, align=center},
        finalbox/.style={rectangle, draw=green!60!black, fill=green!10, thick, minimum height=1.2cm, minimum width=4cm, rounded corners=2mm, align=center},
        arrow/.style={->, >=stealth, thick, color=gray!80!black},
        scorelabel/.style={align=center, font=\small\bfseries, color=red!70!black}
        ]

        % Original token nodes
        \node[box] (card) {\texttt{card}};
        \node[box, right of=card, xshift=1.0cm] (iology) {\texttt{\#\#iology}};
        \node[box, right of=iology, xshift=1.0cm] (visit) {\texttt{visit}};

        % IG Scores (After Step A)
        \node[scorelabel, below of=card, yshift=1.2cm] (scard) {IG: $+12.45$};
        \node[scorelabel, below of=iology, yshift=1.2cm] (siology) {IG: $+7.83$};
        \node[scorelabel, below of=visit, yshift=1.2cm] (svisit) {IG: $+18.92$};

        % The mathematical merge process
        \node[below of=card, xshift=1.75cm, yshift=-0.8cm] (plus) {\LARGE \textbf{$\Sigma \rightarrow$}};

        % Final recomposed nodes
        \node[finalbox, below of=plus, yshift=0.5cm] (final) {\large \textbf{Cardiology}};
        \node[scorelabel, color=green!40!black, below of=final, yshift=1.2cm] (sfinal) {\large Cardiology Score: $+20.28$};
        
        \node[finalbox, below of=visit, yshift=-1.85cm, minimum width=3cm] (finalvisit) {\large \textbf{visit}};
        \node[scorelabel, color=green!40!black, below of=finalvisit, yshift=1.2cm] (sfinalvisit) {\large Visit Score: $+18.92$};

        % Connectors
        \draw[arrow] (scard) -- (plus);
        \draw[arrow] (siology) -- (plus);
        \draw[arrow] (plus) -- (final);
        \draw[arrow] (svisit) -- (finalvisit);
        
    \end{tikzpicture}
    \caption{Ricomposizione post-calcolo della refertazione "Cardiology visit". Il processo addiziona computazionalmente (\textit{Step B}) gli score vettoriali scalari dei \textit{sub-token} frammentati interdipendenti (\texttt{card} e \texttt{\#\#iology}). Il token isolato \texttt{visit} trasferisce a valle la sua integrità quantitativa.}
    \label{fig:token_recomposition}
\end{figure}

% PLACEHOLDER CAPITOLO 04 - PARAGRAFO 4.4 - ISTOGRAMMA TOP ACTIONS
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{img/histogram_actions_narrativo_bert-base-uncased_single_20260226_195041.png}
    \caption{Istogramma logaritmico d'estrazione delle metriche d'impatto clinico sulle \textit{Top Actions} associate dai Gradienti allo spreco del LOS. Il grafico aggrega i dati vettoriali relativi ad un campione mirato di 109 pazienti transitati presso il reparto operativo 701 (Cardiochirurgia) dell'Azienda Ospedaliera di Alessandria. La mancanza di azioni associate alla classe 0 è dovuta al fatto che non ci sono stati pazienti con degenza $\leq 20$ che hanno subito le medesime.}
    \label{fig:actions_histogram}
\end{figure}
\FloatBarrier
\subsection{Applicazione Pratica: Analisi sulle Degenze di Cardiochirurgia}
Il potenziale di questa disaggregazione lessicale è drastico e restituisce istogrammi e \textit{heatmap} (mappe di calore intra-traccia) leggibili, affidabili e matematicamente bilanciate. A titolo dimostrativo (Figure \ref{fig:actions_histogram} e \ref{fig:heatmap}), sono state estratte e renderizzate le logiche attenzionali relative a un pool di 109 degenze afferenti al Reparto 701 di Cardiochirurgia (Ospedale di Alessandria). Attraverso lo studio dei punteggi aggregati sulle esecutive raggruppabili di questo dipartimento, i decisori aziendali sono ora muniti di un cruscotto semantico \textbf{affidabile}. Questo strumento permette di localizzare i veri determinanti processuali che prolungano materialmente le degenze cliniche, \textbf{superando il limite interpretativo tipico dei modelli black-box}.

% PLACEHOLDER CAPITOLO 04 - PARAGRAFO 4.4 - HEATMAP STORYTELLING
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{img/heatmap_actions_narrativo_bert-base-uncased_single_20260226_195041.png}
    \caption{Estratto visivo della heatmap \textit{Storytelling} generata per una traccia in degenza di Cardiochirurgia (Ospedale di Alessandria): l'intensità della sfumatura cromatica isola visualmente e semanticamente lo snodo cronologico ed il collo di bottiglia processuale all'interno del fluire della documentazione del paziente.}
    \label{fig:heatmap}
\end{figure}
\FloatBarrier

\section{Fattibilità Computazionale e Costi di Deployment}
Per valutare la concreta adozione del framework in un ambiente di produzione clinico (deployment aziendale), è stato condotto uno studio parallelo sull'hardware a disposizione e l'investimento di \textit{runtime}. Le prove sono state misurate impiegando un nodo composto da una singola GPU Nvidia RTX 4090, processore AMD Ryzen 9 5900X (12 core, 24 thread) e 64GB di memoria RAM.

La complessità computazionale asintotica della fase di calcolo e di \textit{fine-tuning} incarna la classica topologia delle architetture Transformer. Semplificandone le componenti in base all'architettura interna, risulta esprimibile come:
\begin{equation}
\mathcal{O}(L^2 \cdot H + L \cdot H^2)
\end{equation}
dove $L$ (impostato a $512$ direttamente da Google) riflette la lunghezza massimale della traccia di log passata alla meccanica di \textit{self-attention}, mentre $H$ ($768$ dimensioni in \texttt{bert-base-uncased}) ingombra il computo relativo al layer denso di \textit{feed-forward}.

Al netto del pre-training linguistico intrinseco, lo scoglio di computazione primario per il \textit{deployment} si insinua storicamente nell'innesco degli algoritmi iterativi post-hoc, quali gli \textit{Integrated Gradients}. La complessità estrattiva per l'architettura elaborata è fedelmente approssimabile in:
\begin{equation}
\mathcal{O}(N \cdot K \cdot T \cdot L \cdot H^2)
\end{equation}
dove l'infrastruttura attraversa un bacino di popolarità pari a $N=1109$ campioni in log, processandoli lungo $K=2$ direttrici di estrazione dicotomiche avvalendosi attivamente di un delta fine pari a $T=1500$ steps interpolari. Questi vettori decadranno come poc'anzi sui rami attenzionali base della rete ($L$ ed $H$).

A fronte di questi scenari computazionali formalmente sfidanti, a livello empirico le metriche \textit{wall-clock} registrate sui server in fase sperimentale si sono rivelate altamente compatibili con una pipeline aziendale agile:
\begin{itemize}
    \item \textbf{Generazione delle storie (Parsing XES testuale):} 1 minuto.
    \item \textbf{Fine-Tuning di BERT (su 5 epoche d'addestramento):} 6 minuti e 30 secondi.
    \item \textbf{Estrazione IG estesa (Analisi completa su 109 tracce loggate):} 8 minuti e 16 secondi.
\end{itemize}

Questi riscontri temporali sono risultati sostanzialmente stabili e confrontabili trasversalmente a tutti gli approcci di embedding studiati, a parità di dataset.

Processare analiticamente la degenza ospedaliera e l'impatto clinico interpretato per più di mille pazienti in attesa dimissionaria richiede circa 80 minuti di \textit{overhead}. L'utilizzo differito di tali logiche garantisce all'ecosistema \textit{TEXLOS} un'immediata valenza direzionale. In un framework real-world integrato ad un ospedale, innescando l'esportazione automatizzata in asincrono al termine della fascia notturna tramite un trigger temporizzato sul database asincrono o su istanze \textit{cloud ad-hoc}, è possibile consegnare le liste di monitoraggio priorizzato ed i corrispondenti grafici d'ispezione al *case manager* e ai direttori dipartimentali ad inizio mattinata, preservando i reparti dall'acquisto di hardware di calcolo live sproporzionato ed avvalorando drasticamente i benefici assistenziali attesi all'introduzione empirica.